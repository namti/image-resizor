{
  "name": "image-resizor-online",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "publish": "npm run build && touch ./dist/.nojekyll && gh-pages -d ./dist -t true"
  },
  "devDependencies": {
    "sass": "^1.77.1",
    "typescript": "^5.2.2",
    "vite": "^5.2.0"
  },
  "dependencies": {
    "filesize": "^10.1.2"
  }
}
